import React from 'react';
import ImportQuestions from './ImportQuestions';
export default function Import() {
  return (
    <div>
      <ImportQuestions />
      <h2 className="text-3xl font-semibold mb-4">Pagina Import</h2>
      <p>Qui ci sarà la logica di import dei file, per ora mock.</p>
    </div>
  );
}